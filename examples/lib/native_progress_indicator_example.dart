import 'package:flutter/material.dart';
import 'package:native_widget/native_widget.dart';

class NativeProgressIndicatorExample extends StatelessWidget {
  const NativeProgressIndicatorExample({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: NativeProgressIndicator(),
    );
  }
}
